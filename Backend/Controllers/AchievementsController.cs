using Microsoft.AspNetCore.Mvc;

namespace Backend.Controllers
{
    public class AchievementsController : Controller
    {
        public string Index()
        {
            return "Welcome to achievements controller. Unfortunately I have not realized it yet. :(";
        }
    }
}
