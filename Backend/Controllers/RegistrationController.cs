using Microsoft.AspNetCore.Mvc;

namespace Backend.Controllers
{
    public class RegistrationController : Controller
    {
        public string Index()
        {
            return "Welcome to registration controller. Unfortunately I have not realized it yet. :(";
        }
    }
}
